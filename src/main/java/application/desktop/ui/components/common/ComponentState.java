package application.desktop.ui.components.common;

/**
 * Interaction state of a component.
 */
public enum ComponentState {
    /**
     * Default state of a Component, e.g. when it is not interacting with anything.
     */
    DEFAULT,
    /**
     * When the mouse is hovering over this Component.
     */
    MOUSE_HOVERING,

}
